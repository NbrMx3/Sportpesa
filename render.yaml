services:
  - type: web
    name: sportpesa-api
    env: node
    plan: free
    rootDir: .
    buildCommand: npm install
    startCommand: npm run start
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: JWT_SECRET
        sync: false
      - key: CLIENT_ORIGIN
        sync: false